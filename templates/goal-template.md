---
status: active            # active | done | missed | parked
cycle: YYYY-qN
type: performance         # performance | learning (novel work MUST be learning)
metric:                   # the one number tracked daily/weekly
target:                   # value + date
why:                      # identity-framed, one sentence
---

# G<N>: <title>

## Goal
<Specific, measurable outcome. If it can't be verified true/false on the end date,
rewrite it.>

## Why (identity)
<"I'm the kind of person who…" — not "I should".>

## Anti-goals touched
<Which standing anti-goals this goal is most likely to pressure.>

## Milestones
| # | Milestone | Date | Status |
|---|---|---|---|
| M1 | | | |
| M2 | | | |
| M3 | | | |

## 80% checks
- Makes sense in theory: __% <if <80, simplify>
- Will I actually do it (given my history): __% <if <80, shrink — use the base rate
  in data/profile.md>
- Revision note: <what was cut/shrunk to clear 80>

## WOOP
- **Wish**:
- **Outcome** (best case, one line):
- **Obstacles** (pre-mortem — "week 12, this failed, why?"):
  1.
  2.
  3.
- **Plan** (if-thens — ≥3, covering the obstacle classes in data/profile.md):
  | If | Then |
  |---|---|
  | | |
  | | |
  | | |

<!-- OPERATING PROCEDURE — the machine test:
     Is this goal reached by REPEATING a unit of work (an experiment, a weekly ship, a
     session) whose steps the milestones don't already spell out? If YES, fill the
     section below with the ordered steps to run ONE unit start-to-close. If NO (a
     one-shot deliverable whose milestones already ARE the steps), DELETE this whole
     section — never leave it blank. No unused circuitry. -->
## Operating procedure
<Numbered steps to run ONE unit of this goal, in order: how to start/pick it, how to do
the work, how to close and log it. Concrete enough to execute without re-deciding.
Reference the per-unit ledger/tracking file if the goal has one.>

## System
- Tracked daily: <what one line gets logged>
- Weekly scorecard measure: <how the % is computed>

## Scope changes
<Dated log. Shrinking scope at a weekly review is allowed; silent drift is not.>
